apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-demo-chart.fullname" . }}
  labels:
    {{- include "node-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-demo-chart.selectorLabels" . | nindent 4 }}
