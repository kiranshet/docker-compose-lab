version: "3.9"

services:

  node-app:
    build: .
    container_name: node-app
    ports:
      - "3000:3000"

  nginx:
    image: nginx:latest
    container_name: nginx-app
    ports:
      - "8080:80"
